# ~/.zsh_aliases
# Shell aliases — tracked in git.
# Machine-specific aliases go in ~/.zshrc.local (not tracked in git).

# Editors
alias vim='nvim'

# Git
alias ga='git add'
alias gcmsg='git commit -m'
alias gb='git branch'
alias gd='git diff'
alias gst='git status'

