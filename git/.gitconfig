[user]
    name = Doug James
    email = doug@dwjames.org

[core]
    editor = nvim

[include]
    # Machine-specific overrides (work email etc.) — not tracked in git
    path = ~/.gitconfig.local

[credential]
        helper = !'/usr/local/bin/devpod' agent git-credentials --port 12049
